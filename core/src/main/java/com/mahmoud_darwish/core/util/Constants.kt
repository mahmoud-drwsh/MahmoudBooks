package com.mahmoud_darwish.core.util

object Constants {
    // TODO: this value exists in two different places and this violates the single source of truth principle, thus, find a way to make this declared in only one place and referenced where needed by its variable.
    const val favoritesFeatureModuleName = "ui-favorites"
}